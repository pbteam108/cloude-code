# CLAUDE.md

このファイルはAIアシスタント（Claude等）がこのリポジトリで作業する際のガイドラインを記述しています。

## リポジトリ概要

- **プロジェクト名**: cloude-code
- **リモートURL**: `http://local_proxy@127.0.0.1:34358/git/pbteam108/cloude-code`
- **メインブランチ**: `master`
- **初期コミット日**: 2026-02-15

現在は初期化直後のリポジトリです。ソースコード・テスト・ビルド設定はまだ存在しません。

## ディレクトリ構成

```
cloude-code/
└── README.md   # プロジェクト名のみ記載
```

## 開発ブランチ規約

- ブランチ名は必ず `claude/` で始め、セッションIDで終わる形式にすること
  - 例: `claude/claude-md-mm2t012340a99lhb-OLSEa`
- `master` ブランチへの直接プッシュは行わないこと

## Gitワークフロー

```bash
# ブランチ作成・切り替え
git checkout -b claude/<branch-name>

# プッシュ（必ず -u オプションを使用）
git push -u origin <branch-name>

# プッシュが失敗した場合は指数バックオフでリトライ
# 2s → 4s → 8s → 16s
```

## コミットメッセージ規約

- 変更内容を明確かつ簡潔に記述する
- 日本語・英語どちらも可

## 今後の開発に向けた推奨事項

このリポジトリに技術スタックを追加する場合は、以下を検討してください：

- `package.json` / `requirements.txt` 等の依存関係ファイル
- `.gitignore` の追加
- CI/CDパイプラインの設定（`.github/workflows/` 等）
- テストフレームワークの導入
- コードスタイル設定（ESLint、Prettier、Black 等）

## 注意事項

- 現時点では技術スタック・テスト・ビルドシステムは未設定のため、これらに関する手順はプロジェクト追加後に本ファイルを更新すること
- 本ファイル（CLAUDE.md）はリポジトリの状態が変わるたびに最新情報に更新すること
